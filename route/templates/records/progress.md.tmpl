# Progress

Newest entry at the top, immediately after this header block. Older entries roll into
`PROGRESS_ARCHIVE.md`, prepended so newest-first order holds there too.

---

## 📅 Log: YYYY-MM-DD HH:MM:SS <TZ> (Task 1)

- **Changed**: files, one line each
- **Tests**: n passed, n failed — the command that was run
- **Review**: PASS | FAIL | skipped, and which trigger was checked
- **Accepted risk**: none, or the RISK finding and why it was accepted

<!--
Only facts the dispatch was given. An unknown is `?`, never an estimate.
`bookkeeping.records.progress.keep` decides how many entries stay here before rolling.
Delete this example entry once you have a real one.
-->
